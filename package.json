{
  "name": "scooter-bill-generator-workspace",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm install && cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  }
}
