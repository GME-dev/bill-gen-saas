[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"
dockerfileDirectory = "backend"

[deploy]
startCommand = "node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/api/health"
healthcheckTimeout = 100 